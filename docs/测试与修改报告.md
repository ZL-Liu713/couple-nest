# 恋爱小窝：功能测试与修改报告

状态：核心功能已完成真实双账号复测；新增游乐场完成浏览器验证。邮件送达与线上实际退出归档的覆盖限制见文末。

## 范围与基线

- 线上地址：https://zl-liu713.github.io/couple-nest/
- 当前仓库：ZL-Liu713/couple-nest；基线提交 76561f1。
- 修复分支：codex/full-functional-audit。
- 测试目标：注册、确认邮件、登录、异常恢复、创建与加入双人房间、纪念日、心情、戳戳、留言、共同心愿、邀请、退出、刷新保持、手机布局、网络异常、隐私隔离。
- 使用用户明确授权的两个专用测试账号与测试小窝；报告不记录密码、会话凭证或邀请口令。
- 测试证据分为：线上实测、本机浏览器实测、模拟服务故障测试、真实 SQL 权限测试。模拟测试不代表公网 Supabase 或邮件已经验证。

## 已确认问题

| ID | 严重程度 | 证据 / 重现步骤 | 影响 | 处理状态 |
|---|---|---|---|---|
| F01 | 高 | 线上首页输入悄悄话草稿，再点击「超想你」，文本框变空 | 普通操作丢失未发送内容 | 已修复；回归测试通过 |
| F02 | 高 | 用户已报告 public.members 缺失；代码启动失败仅吐出英文 toast，user 已赋值，界面仍是本机体验 | 不知道是否登录，不能正常恢复 | 已修复；回归测试通过 |
| F03 | 高 | connect() 只要 user 存在就打开配对，无论是否已配对 | 已登录用户误入创建 / 加入界面 | 已修复；回归测试通过 |
| F04 | 高 | 刷新响应没有会话代次校验；退出之后旧响应仍可能覆盖页面 | 退出后旧房间数据重新显示 | 已修复；回归测试通过 |
| F05 | 高 | 写入成功后紧接读取失败被统一提示「没有保存成功」 | 重试可能重复投递 | 已修复；回归测试通过 |
| F06 | 中 | 邀请口令只在当前 URL fragment 中保存；邮箱确认会替换 fragment | 注册后邀请丢失，需要重新找链接 | 已修复；回归测试通过 |
| F07 | 中 | 退出忽略 signOut 返回错误；页面可以显示已退出但会话未清除 | 登录状态失真 | 已修复；回归测试通过 |
| F08 | 中 | 大量主要操作提示为英文，缺少数据库初始化、邮箱未确认和限流的具体指引 | 用户无法自助修复 | 已修复；回归测试通过 |

## 已有修复核实

当前线上及最新源码已包含 emailRedirectTo 和重新发送确认邮件按钮。此前 otp_expired 对应过期 / 已使用确认链接；本轮不打开用户的确认 token。真实邮件链路仍需以新邮件验证。

## 验收记录

持续追加实际测试步骤、结果及证据；不以构建成功替代浏览器或双设备同步验证。

## 本轮实际验收（2026-09-06）

| 测试 | 环境 / 操作 | 结果 |
|---|---|---|
| 已注册邮箱再次注册 | 用户协助，线上点击注册后只出现笼统提示，未收到新邮件 | 已确认提示有歧义；修订版明确区分请求结果和邮件送达 |
| 登录与创建 | 用户登录专用账号，代理创建「功能测试兔」小窝 | 通过，自动出现邀请窗口 |
| 邀请复制 | 用户粘贴后得到原剪贴板中的 JavaScript 源码 | 环境剪贴板不一致；新增可选中口令和手动复制提示 |
| 双人加入 | 用户在第二设备使用真正邀请加入 | 通过；第一设备无需刷新显示第二位成员 |
| 双向留言 / 心愿 / 心情 | 第二设备添加数据，第一设备自动显示；代理回发验证留言 | 接收端浏览器证据通过；反向由用户确认无需刷新收到 |
| 戳戳 | 第一设备发送，用户确认第二设备收到并戳回 | 复核通过：第二测试账号发送后，第一账号无需刷新显示收到 1 次想念 |
| 保存与刷新 | 创建留言、心愿，纪念日设 2026-09-01，刷新 | 留言、心愿、纪念日保留；显示第 6 天；登录恢复 |
| 草稿丢失回归 | 修订版本机输入草稿，切换心情与留言页 | 草稿保持；提交后仅清空已提交内容 |
| 心愿完成 | 本机添加心愿、勾选、刷新 | 完成状态保持 |
| 手机布局 | 390×844，首页、设置弹窗、相册 | 可操作；弹窗可滚动；已保存截图 |
| 本机头像与性别 | 选女、狐狸头像并保存 | 保存后首页显示狐狸头像 |
| 本机照片处理 | 选择测试 PNG、填写说明、收藏 | 显示 1 张照片；仅本机体验，不上传 |
| 相册展示 | 切换拍立得 / 整齐画廊，点击大图 | 可切换，大图可打开和关闭 |
| 数据库 v2 | PGlite 执行升级两次，测试私密照片、头像、退出归档 | 通过；测试包含真实 SQL 与模拟 storage 表，非真实对象存储服务 |

截图：
- `output/playwright/online-persistence.png`：线上数据刷新保持。
- `output/playwright/mobile-home.png`：手机首页。
- `output/playwright/photo-lightbox.png`：本机照片大图。

## 修改说明

1. 草稿独立于页面渲染保存，切页、心情和数据更新不再直接清空；发送期间继续输入的内容不会被先前请求清除。
2. 区分未登录、读取中、待配对、就绪、错误状态；数据库缺表时给出升级和重试入口。
3. 已配对账号点击连接状态进入设置，不再误入创建小窝。
4. 读取带会话代次校验，退出后迟到的请求不再把旧房间渲染回来。
5. 写入使用数据库返回记录更新页面，不把后续读取错误当成写入失败；失败可重试。
6. 邀请在当前标签页保存，确认邮件导致 fragment 改变时仍可找回；只接受有效 UUID / 邀请网址。
7. 注册与重发提示更明确，常见认证、限流、数据库和网络报错改为可行动的中文说明。
8. 头像可选 8 种，性别可选不展示 / 女 / 男 / 其他。注册时选择，配对时确认，设置中可修改。
9. 新增私密相册：单张 5 MB 内 JPG/PNG/WebP，解码后转为最长边 1800px JPEG，去除相机元数据；使用私有桶与有时效的访问链接。
10. 照片说明最长 120 字，相册加载最近 60 张；拍立得、整齐画廊与大图浏览。
11. 退出账号单独提供；退出小窝要求输入确认语。退出会归档原小窝，保留历史，禁止再邀请新人，避免旧内容暴露给新配对者。

## 部署顺序与回滚

先运行 `supabase/upgrade-v2.sql`，再部署前端。用户已确认升级成功；真实私有相册上传及第二账号查看已经复测通过。

SQL 升级不删除历史数据。旧版前端可以继续读取原有字段，但不能使用新增资料 / 相册功能。前端回滚可部署上一提交。已执行的退出小窝属于用户行为，不应通过回滚前端恢复成员权限。

## 当前限制与尚未完成

- 不提供系统推送；未打开网页的用户下次登录才能看到持久化留言。连接状态不代表伴侣在线，也不是已读回执。
- 尚未验证全新邮箱的 SMTP 送达率；重复注册邮箱不代表会再次收到邮件。
- 真实退出归档未对当前双人测试小窝执行，以保留两人配对状态。确认语拦截 / 取消流程已在浏览器通过；离开后隔离、归档和禁写通过真实 SQL 的 PGlite 测试。
- 不包括相册分页、照片删除、视频和语音上传。照片删除若后续增加，需有确认与数据一致性处理。
- 三连棋为同屏双人，翻翻乐为单人；异地互动通过发送问答、成绩及留言完成。没有宣称棋盘实时联机。


## 追加验收：真实服务与游乐场

两个独立登录会话分别运行在生产站和本机测试站，均连接同一个真实 Supabase；另有用户之前的第二设备回执。

| 测试 | 实际结果 | 证据级别 |
|---|---|---|
| 云端照片上传 | 第二账号上传非敏感测试截图并填写说明；第一账号相册自动出现照片，图片解码成功 | 真实服务 + 浏览器 |
| 云端头像保存 | 第二账号选择狐狸头像、不展示性别并保存；第一账号同步显示狐狸 | 真实服务 + 浏览器 |
| 退出登录 | 第二账号退出后刷新，仍为未登录体验状态；重新登录恢复原小窝 | 真实服务 + 浏览器 |
| 退出小窝防误触 | 不输入确认语直接提交，被中文提示拦截；关闭弹窗后仍留在原小窝 | 浏览器 |
| 问答卡 | 发送问题后第一账号信箱收到，含卡片主题和完整问题 | 真实服务 + 浏览器 |
| 翻翻乐 | 通过可见卡面完成全部 8 对；分享 16 次翻牌成绩，第一账号收到 | 浏览器 + 真实服务 |
| 爱心与星星 | 按 1、4、2、5、3 格落子，爱心三连判胜，棋盘停止继续落子 | 浏览器 |
| 设置入口 | 发现桌面矮窗口菜单过高挤出设置；修复侧栏滚动和短窗口布局，设置和退出按钮实测可点 | 浏览器 |
| 自动化检查 | 16 项测试全部通过，生产构建成功 | Node + PGlite |

新增截图位于本机 `output/playwright/games-final.png`、`games-completed.png`。包含测试房间内容的截图未加入公开仓库。

### 发布记录

- 资料、相册、退出和恢复能力：提交 `72d9a3c`，GitHub Actions [33977623137](https://github.com/ZL-Liu713/couple-nest/actions/runs/33977623137) 成功，已线上验证。
- 游乐场和短窗口设置入口修复：随本报告更新提交，自动通过 main 分支工作流发布。

### 验收边界

功能成功并不代表邮箱供应商送达率已经验证。本次用的是已确认测试账号，未请求用户在睡眠期间再次协助确认新邮件。任何新注册问题应根据认证日志和新邮件结果继续定位。数据库权限测试覆盖跨房间读取和伪造写入；实际存储上传验证使用授权测试账号，不向公开桶迁移照片。
